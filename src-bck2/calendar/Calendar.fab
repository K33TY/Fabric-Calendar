package calendar;

import fabric.util.Map;
import fabricated.util.HashMap;

// Principal p is the servlet principal, this is the principal p's current session calendar
public class Calendar[principal p] extends AbstractPrincipal
{
	Calendar{this←}()
	{
		super("calendar");
	}

	//protected final
	// NOT SURE IF I CAN USE HASHMAPS, WOULD BE NICE
	//HashMap<int, Event>{this} ownerEvents = new HashMap<int, Event>{this}();
}