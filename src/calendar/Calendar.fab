package calendar;

import fabric.util.Map;
import fabricated.util.HashMap;

// Calendar is an abstract principal representing the "servlet principal," which is fully trusted
// It is instantiated with the servlet principal label
public class Calendar[principal p] extends AbstractPrincipal
{
	Calendar{this←}()
	{
		super("calendar");
	}

	//TODO: list of users, current user, list of events
}